<!--components/area_header/area_header.wxml-->
<view class="header">
  <view class="title">{{title}}</view>
  <view class="more" bindtap="onMoreTap" wx:if="{{hasMore}}">
    <text class="text">更多</text>
    <van-icon name="arrow"></van-icon>
  </view>
</view>